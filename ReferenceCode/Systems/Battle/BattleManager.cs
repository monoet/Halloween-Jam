using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Legacy/ATB placeholder manager renamed to avoid conflict with new Combat system BattleManager
[AddComponentMenu("Legacy/ATB BattleManager (Legacy)")]
public class ATBBattleManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
