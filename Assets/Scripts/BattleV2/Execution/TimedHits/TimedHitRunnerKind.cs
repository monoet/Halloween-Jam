namespace BattleV2.Execution.TimedHits
{
    /// <summary>
    /// Identifies which runner should evaluate a timed-hit request.
    /// </summary>
    public enum TimedHitRunnerKind
    {
        Default = 0,
        Basic = 1
    }
}
